const { expect } = require('chai');

// Esempio di test unitario
describe('Test Unitari - Backend', () => {
  it('dovrebbe passare un test di esempio', () => {
    expect(true).to.be.true;
  });

  it('dovrebbe sommare due numeri', () => {
    expect(1 + 1).to.equal(2);
  });
}); 
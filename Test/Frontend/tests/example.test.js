// Esempio di test per il frontend

describe('Test Frontend', () => {
  test('dovrebbe passare un test di esempio', () => {
    expect(true).toBe(true);
  });

  test('dovrebbe concatenare stringhe', () => {
    expect('Hello' + ' ' + 'World').toBe('Hello World');
  });
}); 